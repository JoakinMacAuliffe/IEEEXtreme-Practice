#include <bits/stdc++.h>

using namespace std;

using ll = long long;
using ld = long double;

bool canFormChain(vector<pair<int,int>>& dominoes) {
    if(dominoes.empty)
}

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t, m;
    cin >> t;

    while(t--) {
        cin >> m;
        while(m--) {
            int d[m]
        }
    }

    return 0;
}