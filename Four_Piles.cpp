#include <bits/stdc++.h>

using namespace std;

using ll = long long;
using ld = long double;

ll mod(ll n, ll p) {
    return ((n%p) + p)%p;
}

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int Q;
    while(Q--) {
        ll A1, B1, A2, B2, A3, B3, A4, B4;
        cin >> A1 >> B1 >> A2 >> B2 >> A3 >> B3 >> A4 >> B4;
        
    }

    

    return 0;
}