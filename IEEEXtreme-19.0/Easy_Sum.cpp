#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int n, k;
    int A[n];
    for(int i=0; i<n; i++) {
        cin>>A[i];
    }
    
    for(int i=1; i<=k; i++) {
        int S[k];
        S[k] = 0;
        // Desde cada posicion final r desde l hasta
    }

    }

    return 0;
}