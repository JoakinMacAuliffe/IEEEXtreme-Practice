    #include <bits/stdc++.h>

    using namespace std;

    using ll = long long;
    using ld = long double;

    int main()
    {

        std::ios_base::sync_with_stdio(false);
        std::cin.tie(NULL);

        int n, k;
        int S[n];
        for(int i=0; i<n; i++) {
            cin >> S[i];
        }

        return 0;
    }